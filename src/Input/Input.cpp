#include "Input.h"

namespace engine {
    void Input::initialize() {
        // Initialize the renderer
    }

    void Input::update() {
        // Update the renderer
    }

    void Input::shutdown() {
        // Shutdown the renderer
    }
}
